<%= render "components/header" %>
<% if user_signed_in? %>
<div class="btn btn-dark btn-banner-p"><%= link_to "Ajouter un Poète", new_poete_path %></div>
<% end %>
<div class="index_2">

<p class="intro-poete">Le Gué-savoir s'inspire du Consistori del gay saber en 1323, ancêtre de l’Académie des jeux floraux de Toulouse,  témoignage de la dimension intrinsèquement métatextuelle et réflexive de la poésie troubadouresque, pour construire une communauté de poètes contemporains, liés entre eux par la  la notion de fraternité, fraternité de la condition humaine, mais aussi fraternité linguistique des langues romanes. Le choix des poètes porte sur leurs relations aux lieux, aux paysages, aux langues régionales qui sont à l'origine de leurs paroles poétiques. C'est une errance de poésies qui va de la Galice aux pays d'oc.</p>

<div class="display-grid-card-poete">
  <% @poete.each do |poete| %>
    <div class="trajet">
      <% if user_signed_in? %>
        <div class ="btn-trajet">
          <div class="btn btn-danger btn-banner-p"><%= link_to 'Supprimer', poete_path(poete),
            method: :delete,
            data: { confirm: 'Êtes-vous certain de vouloir supprimer?' } %></div>
          <div class="btn btn-success btn-banner-p"><%= link_to "Editer", edit_poete_path(poete.id)%></div>
        </div>
      <% end %>
          <div class="img-poete">
            <%= link_to(image_tag("#{poete.image_poete_1}", alt: "Diogo Maia Moreira"), poete_path(poete.id)) %>
            <h1><%= poete.titre_poete %></h1>
            <div class="info_p">
            <h4 class=""><%= poete.date_poete %></h4>
            <h5 class=""><%= poete.lieu_poete %></h5>
            </div>
          </div>
    </div>
     <% end %>
  </div>

</div>
<%= render "components/footer" %>

