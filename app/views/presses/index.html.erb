<%= render "components/header" %>
<body style="background-image: url('https://res.cloudinary.com/dft5iblyz/image/upload/v1611095330/1_jkgddt.jpg'); background-size: cover;">
<% if user_signed_in? %>
<div class="btn btn-dark btn-banner-p"><%= link_to "Ajouter une Action", new_press_path %></div>
<% end %>
<div class="index_2">

<div class="display-grid-card-action">
  <% @presse.each do |presse| %>
    <div class="trajet">
      <% if user_signed_in? %>
        <div class ="btn-trajet">
          <div class="btn btn-danger btn-banner-p"><%= link_to 'Supprimer', press_path(presse),
            method: :delete,
            data: { confirm: 'Êtes-vous certain de vouloir supprimer?' } %></div>
          <div class="btn btn-success btn-banner-p"><%= link_to "Editer", edit_press_path(presse.id)%></div>
        </div>
      <% end %>
          <div class="img-poetess">
            <%= link_to(image_tag("#{presse.image_presse}", alt: "Diogo Maia Moreira"), press_path(presse.id)) %>
            <h1><%= presse.titre_presse %></h1>
            <div class="info_p">
            <h4 class=""><%= presse.date_presse %></h4>
            <h5 class=""><%= presse.lieu_presse %></h5>
            </div>
          </div>
    </div>
     <% end %>
  </div>

</div>
</body>
<%= render "components/footer" %>

